<%_* 
let text = await tp.system.clipboard();
text = text.replace(/(\r\n|\n|\r)/g, ' | '); // Nahradí všechny druhy zalomení řádků mezerami
text = text.replace(/(\t)/g, ' '); // Nahradíme tab
tR = text;
_%>
